/*
 * DialogOption Messages
 *
 * This contains all the text for the DialogOption component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DialogOption';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DialogOption component!',
  },
});
