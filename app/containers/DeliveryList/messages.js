/*
 * DeliveryList Messages
 *
 * This contains all the text for the DeliveryList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DeliveryList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeliveryList container!',
  },
});
